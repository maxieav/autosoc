[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "autosoc"
version = "0.1.0"
description = "Automated Security Operations Center tool"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
autosoc = "autosoc.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
